//Version for FreeBSD

// lists all Stats you can get
list = [ "load" , "HDDtemp" , "HDDusage" , "liveNet" , "cpu" , "login"];


//Path to the json Files
//WITH trailing / IMPORTANT
path = "json/";


//enabled:  shall script check this Stat
//type:     
//      0:  Line Graph       (Shows values over Time)
//      1:  Bar Graph ( (Used Disk space etc)
//      2:  CSV Table   (Shows Table of Data)
//cmmd:     command to get the status
//regex:    which regular Expression should be applied
//If Type = 2, output is not regexed, the regex value is used as Header for the Table

//CPU load
load =
{
    enabled = true;
    type = 0;
    cmmd = "uptime";
    regex = "[0-9]?[0-9],[0-9][0-9]";
}

//HDD Temperatures
HDDtemp = 
{
    enabled = true;
    type = 0;
    cmmd = 
     "smartctl -A /dev/ada0 | awk '/Airflow_Temperature_Cel/{print $0}' | awk '{print $10}'";
    regex = "[0-9]+";
}

//Disk Usage
HDDusage = 
{
    enabled = false;
    type = 1;
    cmmd = 
         "df | grep ^/dev/ada0p2 | awk '{print $3}'";
    regex = "[0-9]+";
}

//NetworkTraffic
liveNet =
{
    enabled = true;
    type = 0;
    cmmd = "ifstat -i re0 1 1 | tail -1";
    regex = "[0-9]?[0-9]\\.[0-9][0-9]";
}

//CPU Temperature
cpu =
{
    enabled = true;
    type = 0;
    cmmd = "sysctl -a hw.acpi.thermal | grep temperature";
    regex = "[0-9][0-9]\\.[0-9]"
}

//Table of last logins
login = 
{
    enabled = true;
    type = 2;
    cmmd = "last | head -10 | grep -v ^reboot | grep -v wtmp | grep -v ^$ | grep -v ^$ | awk '{ if($1!=\"\") gsub(\"logged\", \"Now\", $9); print $1 \";\" $7 \";\" $9 \";\"}'";
    regex = "User; Von; Bis";
}
